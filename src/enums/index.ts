export enum Enabled {
  TRUE = 1,
  FALSE = 2,
}

export enum Visiable {
  TRUE = 1,
  FALSE = 2,
}

export enum OrderTypes {
  DESC,
  ASC,
}

export enum MenuTypes {
  LAYOUT = 1,
  API = 2,
  ROUTE = 2,
}
